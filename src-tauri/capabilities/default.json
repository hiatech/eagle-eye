{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the primary Eagle Eye window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
